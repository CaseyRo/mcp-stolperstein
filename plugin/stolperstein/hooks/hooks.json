{
  "PostToolUse": [
    {
      "matcher": "Bash",
      "hooks": [
        {
          "type": "command",
          "command": "if echo \"$TOOL_OUTPUT\" | grep -qiE '(error|failed|fatal|exception|traceback)'; then echo '{\"query\": {\"text\": \"'$(echo \"$TOOL_OUTPUT\" | head -5 | tr '\\n' ' ' | sed 's/\"/\\\\\"/g')'\", \"limit\": 3}}'; fi",
          "timeout": 5000
        }
      ]
    }
  ]
}
